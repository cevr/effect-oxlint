---
"oxlint-plugin-effect": minor
---

Expand Effect program design guidance. Detect manual tagged control flow, unchecked inline service implementations, hidden provisioning, nested generators, unnamed operations, discarded serial aggregation, silent failure erasure, per-call cache construction, unbounded stream collection, unbounded concurrency, unbounded retry schedules, and nested `ManagedRuntime` construction.
