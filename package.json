{
  "name": "effect-oxlint",
  "private": true,
  "packageManager": "bun@1.3.11",
  "workspaces": ["packages/oxlint-plugin-effect"],
  "scripts": {
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "lint:ox": "oxlint",
    "test": "turbo run test",
    "build": "turbo run build",
    "gate": "concurrently -n js,go -c blue,green \"bun run gate:js\" \"bun run gate:go\"",
    "gate:js": "cd packages/oxlint-plugin-effect && bunx tsgo --noEmit && bun test",
    "gate:go": "cd packages/tsgolint-effect && go build -o /dev/null ./cmd/tsgolint && go test ./internal/rules/effect_.../",
    "codegen": "bun run packages/oxlint-plugin-effect/scripts/codegen.ts",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "concurrently": "latest",
    "lefthook": "latest",
    "turbo": "latest"
  }
}
